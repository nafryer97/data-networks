#!/bin/zsh

for i in {0..1024}
do
    echo "$i: This is a dummy input file for data networks project 4. The project is a file transfer program that transfers a file from a sender to a receiver using UDP datagram messages. Each of the lines in this file should be reconstructed at the receiver in order." >> itest
done
